services:
  api:
    volumes:
      # Relative to the project directory (root docker-compose.yml), not this file.
      - .:/workspace:cached
    working_dir: /workspace/backend
    user: root
    restart: "no"
